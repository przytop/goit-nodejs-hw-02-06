# This is an example .env file for a Node.js application
# Copy this file to .env and fill in your own values

# MongoDB connection URI (required)
MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/your-dbname

# Port number the server listens on (default: 3000)
PORT=3000

# JWT secret key (required, use a strong, random string)
JWT_SECRET=your_jwt_secret_key