# Application Environment Configuration
# Copy this file to ".env" and replace placeholder values with your own.

# MongoDB connection URI (required)
MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/your-dbname

# Port number the server listens on (default: 3000)
PORT=3000

# JWT secret key (required, use a strong, random string)
JWT_SECRET=your_jwt_secret_key

# API key from SendGrid (required)
SENDGRID_API_KEY=your_sendgrid_api_key

# Email verified in SendGrid to send from (required)
SENDGRID_SENDER_EMAIL=your_verified_sender_email
